---
title: Configuration
description: Configure the JouleBridge runtime for real site deployments.
---

# Configuration

Configuration exists to make the site runtime predictable, repeatable, and safe to operate.

The main areas are:

- site identity
- adapter definitions
- policy bundles
- environment overlays

Read the linked pages before running a pilot deployment.
