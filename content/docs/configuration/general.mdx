---
title: General Configuration
description: General runtime configuration for site identity, deployment shape, and local services.
---

# General Configuration

General configuration should define:

- site identity
- active runtime profile
- service boundaries
- storage and export paths

The key rule is simple: the local site must remain supportable even if cloud connectivity is not.
